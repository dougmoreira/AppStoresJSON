# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AppStore Using JSON API' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AppStore Using JSON API
  pod 'SDWebImage'

  target 'AppStore Using JSON APITests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AppStore Using JSON APIUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
